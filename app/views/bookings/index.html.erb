
<% @bookings.each do |booking| %>
      <p> Booking number <%= link_to booking.id, booking_path(booking) %>  for  <%= booking.user.email %> at : <%= booking.space.name %>
      <br>
  <% end %>
  <%= link_to 'Back to spaces list', spaces_path %>
